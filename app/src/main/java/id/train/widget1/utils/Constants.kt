package id.train.widget1.utils

object Constants {
    private const val PATH = "id.train.widget1.action."

    const val CONNECT_DISCONNECT = PATH + "CONNECT_DISCONNECT"
    const val BROADCAST_STATUS = PATH + "BROADCAST_STATUS"
    const val VPN_STATE_CHANGE = "VPN_STATE_CHANGE"

    const val NOTIFICATION_KEY = "NOTIFICATION_KEY"
}